class ToDo {
    constructor(id, titre, done) {
        this.id = id;
        this.titre = titre;
        this.done = done;
    }

    check() {
        // to do
        // evenement -> marque done à true
        // si done -> indicateur visuel que tâche faite
    }

}